<Page x:Class="InternalBaseWpf.Pages.DuplicatesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Дубли"
      Loaded="Page_Loaded">

    <Grid Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Дубли" FontSize="20" Margin="20,20,20,10" />

        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Margin="20,0,20,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="btnRefresh" Click="BtnRefresh_Click" Margin="5,0" ToolTip="Обновить" Style="{StaticResource MaterialDesignFlatButton}">
                        <materialDesign:PackIcon Kind="Refresh" Width="22" Height="22" />
                    </Button>
                    <Button x:Name="btnDelete" Click="BtnDelete_Click" Margin="5,0" ToolTip="Удалить выбранный дубль" Style="{StaticResource MaterialDesignFlatButton}">
                        <materialDesign:PackIcon Kind="Delete" Width="22" Height="22" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Margin="20,0,20,10" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid x:Name="dgDuplicates"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserReorderColumns="True"
                      CanUserResizeColumns="True"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Stretch"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="colDuplicateKey" Header="Группа дублей" Binding="{Binding DuplicateKey}" Width="*" MinWidth="220" />
                    <DataGridTextColumn x:Name="colLocalBranch" Header="Местное отделение" Binding="{Binding LocalBranch}" Width="*" MinWidth="200" />
                    <DataGridTextColumn x:Name="colFullName" Header="ФИО" Binding="{Binding FullName}" Width="*" MinWidth="150" />
                    <DataGridTextColumn x:Name="colBirthDate" Header="Дата рождения" Binding="{Binding BirthDateText}" Width="Auto" MinWidth="110" />
                    <DataGridTextColumn x:Name="colPhone" Header="Моб. телефон" Binding="{Binding Phone}" Width="Auto" MinWidth="120" />
                    <DataGridTextColumn x:Name="colAddress" Header="Адрес" Binding="{Binding Address}" Width="*" MinWidth="200" />
                    <DataGridTextColumn x:Name="colCreatedAt" Header="Дата создания" Binding="{Binding CreatedAtText}" Width="Auto" MinWidth="150" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="20,0,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Строк на странице:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <ComboBox x:Name="cmbPageSize" Width="40" SelectedIndex="0" SelectionChanged="CmbPageSize_SelectionChanged">
                    <ComboBoxItem>25</ComboBoxItem>
                    <ComboBoxItem>50</ComboBoxItem>
                    <ComboBoxItem>100</ComboBoxItem>
                </ComboBox>
            </StackPanel>

            <TextBlock Grid.Column="1" x:Name="txtStatus" Text="Загрузка..." HorizontalAlignment="Center" VerticalAlignment="Center" />

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="btnFirst" Content="|&lt;" Click="BtnPage_Click" Tag="First" Margin="2,0" Foreground="White"/>
                <Button x:Name="btnPrev" Content="&lt;" Click="BtnPage_Click" Tag="Prev" Margin="2,0" Foreground="White"/>
                <TextBlock x:Name="txtPageInfo" Text="1" VerticalAlignment="Center" Margin="5,0" />
                <Button x:Name="btnNext" Content=">" Click="BtnPage_Click" Tag="Next" Margin="2,0" Foreground="White"/>
                <Button x:Name="btnLast" Content=">|" Click="BtnPage_Click" Tag="Last" Margin="2,0" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
