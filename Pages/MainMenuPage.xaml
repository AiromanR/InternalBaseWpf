<Page x:Class="InternalBaseWpf.Pages.MainMenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1400"
      Title="Внутренняя база">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="pack://application:,,,/Images/logo.png" Width="44" Height="44" Margin="0,0,12,0" />

                <TextBlock Grid.Column="1" Text="Внутренняя база" FontSize="22" VerticalAlignment="Center" Foreground="{StaticResource TextBrush}" />

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button x:Name="btnTouches" Content="КАСАНИЯ" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{StaticResource MutedTextBrush}" Click="NavButton_Click" Tag="Touches" FontSize="13" />
                    <Button x:Name="btnActivists" Content="АКТИВИСТЫ" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{StaticResource MutedTextBrush}" Click="NavButton_Click" Tag="Activists" FontSize="13" />
                    <Button x:Name="btnCells" Content="ЯЧЕЙКИ" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{StaticResource MutedTextBrush}" Click="NavButton_Click" Tag="Cells" FontSize="13" />
                    <Button x:Name="btnUnconfirmed" Content="НЕПОДТВЕРЖДЕННЫЕ" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{StaticResource MutedTextBrush}" Click="NavButton_Click" Tag="Unconfirmed" FontSize="13" />
                    <Button x:Name="btnDuplicates" Content="ДУБЛИ" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{StaticResource MutedTextBrush}" Click="NavButton_Click" Tag="Duplicates" FontSize="13" />
                    <Button x:Name="btnAdmin" Content="АДМИНИСТРАТОР" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{StaticResource MutedTextBrush}" Click="NavButton_Click" Tag="Admin" Visibility="Collapsed" FontSize="13" />
                </StackPanel>

                <TextBlock Grid.Column="3" x:Name="txtUserName" VerticalAlignment="Center" Margin="25,0,10,0" Foreground="{StaticResource TextBrush}" FontSize="16" />

                <Button Grid.Column="4" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnExit_Click" ToolTip="Выход">
                    <materialDesign:PackIcon Kind="Logout" Foreground="{StaticResource MutedTextBrush}" Width="24" Height="24" />
                </Button>
            </Grid>
        </Border>

        <Frame Grid.Row="1" x:Name="ContentFrame" NavigationUIVisibility="Hidden" />
    </Grid>
</Page>
