<Page x:Class="InternalBaseWpf.Pages.CellsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Ячейки"
      Loaded="Page_Loaded">

    <Grid Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ячейки активистов" FontSize="20" Margin="20,20,20,10" />

        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Margin="20,0,20,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch"
                         Grid.Column="0"
                         materialDesign:HintAssist.Hint="Поиск"
                         TextChanged="TxtSearch_TextChanged" />

                <Button Grid.Column="1" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnRefresh_Click" Margin="5,0">
                    <materialDesign:PackIcon Kind="Refresh" Width="22" Height="22"/>
                </Button>

                <Button Grid.Column="2" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnAdd_Click" Margin="5,0,0,0">
                    <materialDesign:PackIcon Kind="Plus" Width="22" Height="22"/>
                </Button>

                <Button Grid.Column="3" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnDelete_Click" Margin="5,0,0,0"  ToolTip="Удалить">
                    <materialDesign:PackIcon Kind="Delete" Width="22" Height="22" />
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2" Margin="20,0,20,10" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <ListBox x:Name="lbCells"
                     BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Изменить" Click="BtnEdit_Click" />
                                    <MenuItem Header="Удалить" Click="BtnDelete_Click" />
                                </ContextMenu>
                            </Grid.ContextMenu>
                            <TextBlock Text="{Binding Name}" Padding="5" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        
    </Grid>
</Page>
