<Page x:Class="InternalBaseWpf.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Вход в систему">

    <Grid Background="White">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="420">

            <Border Background="#fff" CornerRadius="12" Padding="20" HorizontalAlignment="Center" Margin="0,0,0,25">
                <Image Source="pack://application:,,,/Images/userlist.png"
                       Width="220" Height="220"
                       Stretch="Uniform" />
            </Border>

            <TextBlock Text="ДОБРО ПОЖАЛОВАТЬ ВО ВНУТРЕННЮЮ БАЗУ"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,0,0,35" />

            <TextBox x:Name="txtLogin"
                     materialDesign:HintAssist.Hint="Пользователь"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,15" />

            <PasswordBox x:Name="txtPassword"
                         materialDesign:HintAssist.Hint="Пароль"
                         Style="{StaticResource MaterialDesignOutlinedRevealPasswordBox}"
                         Margin="0,0,0,25" />

            <Button x:Name="btnLogin"
                    Content="ВОЙТИ В СИСТЕМУ"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{StaticResource PrimaryBlue}"
                    Foreground="White"
                    Height="45"
                    Click="BtnLogin_Click" />
        </StackPanel>
    </Grid>
</Page>
