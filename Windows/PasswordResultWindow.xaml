<Window x:Class="InternalBaseWpf.Windows.PasswordResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Пароль создан" Height="220" Width="420"
        WindowStartupLocation="CenterScreen"
        Icon="/Images/key.ico"
        ResizeMode="NoResize">

    <Grid Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Header}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" Margin="25,25,25,10" />

        <TextBlock Grid.Row="1" Text="Сгенерированный пароль:" Margin="25,0,25,5" Foreground="{StaticResource TextBrush}" />

        <Grid Grid.Row="2" Margin="25,0,25,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtPassword" Grid.Column="0" IsReadOnly="True" Text="{Binding Password, Mode=OneWay}" FontFamily="Consolas" FontSize="14" Padding="10,5" VerticalContentAlignment="Center" />

            <Button Grid.Column="1" x:Name="btnCopy" Click="BtnCopy_Click" Margin="10,0,0,0" ToolTip="Скопировать пароль" Style="{StaticResource MaterialDesignFlatButton}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ContentCopy" Margin="0,0,5,0" />
                    <TextBlock Text="КОПИРОВАТЬ" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>

        <Button Grid.Row="3" Content="OK" HorizontalAlignment="Right" Margin="25,20,25,25" Width="100" Click="BtnOk_Click" Style="{StaticResource MaterialDesignRaisedButton}" Background="{StaticResource PrimaryBlue}" Foreground="White" />
    </Grid>
</Window>
