<Window x:Class="InternalBaseWpf.Windows.ColumnSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Настройка столбцов" Height="600" Width="550"
        WindowStartupLocation="CenterScreen"
        Icon="/Images/cog.ico"
        ResizeMode="CanResize"
        MinWidth="350"
        MinHeight="300">

    <Grid Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="НАСТРОЙКА СТОЛБЦОВ" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" Margin="25,25,25,15" />

        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="25,0,25,0" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListBox x:Name="lbColumns" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Header}" IsChecked="{Binding IsVisible}" FontSize="14" Padding="8,4" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="25,20,25,25">
            <Button Content="ОТМЕНА" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{StaticResource ErrorBrush}" Click="BtnCancel_Click" Margin="0,0,15,0" />
            <Button Content="СОХРАНИТЬ" Style="{StaticResource MaterialDesignRaisedButton}" Background="{StaticResource PrimaryBlue}" Foreground="White" Width="150" Click="BtnSave_Click" />
        </StackPanel>
    </Grid>
</Window>
